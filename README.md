# json-snippets-collection
json-snippets-collection
# json-snippets-collection
A small collection of reusable JSON snippets for demos, tests, and prototypes.

## Folders
- `users/` — example user data.
- `config/` — app configs & feature toggles.
- `data/` — product examples.
